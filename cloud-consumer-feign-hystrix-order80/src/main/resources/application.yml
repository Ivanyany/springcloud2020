server:
  port: 80

#eureka配置
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/


#hystrix服务降级
feign:
  hystrix:
    enabled: true
